name: responsive_ui_flutter
description: A Flutter library for detecting runtime environments and building responsive interfaces based on the available viewport.
version: 0.0.1
repository: https://github.com/arvrdnatal/responsive_ui_flutter.git

environment:
  sdk: ^3.7.2
  flutter: ">=3.29.3"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0